# models/

**Purpose**
Storage for saved, trained model artifacts ready for evaluation, inference,
or sharing. Large binary files are git-ignored.

**Future Contents**
- Serialized trained models.
- Exported / packaged model weights.
- Model cards and metadata.
